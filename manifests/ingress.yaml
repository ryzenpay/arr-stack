apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: media-ingress
  namespace: media
spec:
  ingressClassName: traefik
  rules:
  - host: sonarr.media.ryzen.me
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: media-service
            port:
              number: 8989
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lidarr.media-service
            port:
              number: 8686
      - path: /
        pathType: Prefix
        backend:
          service:
            name: radarr.media-service
            port:
              number: 7878
